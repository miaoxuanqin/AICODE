# 智能助手-向量语义搜索-详细设计

> 更新日期：2026-04-27

## 1. 功能概述

三个智能助手（问答助手、执法智能助手、工程监管助手）统一采用**向量语义搜索**技术，实现对用户问题的智能问答。与传统关键词搜索不同，向量搜索能够理解语义关联，即使表述不同但含义相似的问题也能被正确检索。

## 2. 技术方案

### 2.1 向量搜索流程

```
用户问题
    ↓
Embedding 模型转向量 (text2vec-base-chinese, 768维)
    ↓
Qdrant 向量相似度搜索
    ↓
返回最相似的知识片段
    ↓
构建 Prompt → LLM 生成回答
```

### 2.2 统一搜索策略（2026-04-27 更新）

**所有助手统一使用向量搜索，失败时 fallback 到关键词搜索：**

```python
# 1. 检索相关知识
# 所有助手都优先使用向量搜索（语义搜索），失败则 fallback 到关键词搜索
try:
    from app.services.embedding_service import embedding_service
    query_vector = embedding_service.encode_single(question)
    knowledge_ids = self.search_service.search_vector(
        query_vector=query_vector,
        user_id=user_id,
        limit=search_size
    )
    # 根据 ID 获取完整知识信息
    items = []
    for kid in knowledge_ids:
        doc = self.search_service.get_by_id(kid)
        if doc:
            items.append({...})
    search_results = {"items": items, "total": len(items)}
except Exception as e:
    print(f"向量搜索失败，fallback 到关键词搜索: {e}")
    search_results = self.search_service.search_keyword(
        query=question,
        user_id=user_id,
        page=1,
        page_size=search_size
    )
```

### 2.3 搜索方式对比

| 搜索类型 | 原理 | 优势 | 劣势 |
|----------|------|------|------|
| **向量搜索** | 语义理解，将文本转为向量计算相似度 | 同义词/相关概念能关联 | 需额外 Embedding 服务 |
| **关键词搜索** | BM25 评分，精确匹配关键词 | 精确匹配，速度快 | 字面不符则搜不到 |

### 2.4 演示：向量搜索 vs 关键词搜索

**问题：** `楼板出现裂缝怎么处置？`

| 搜索类型 | 结果 | 说明 |
|----------|------|------|
| **向量搜索** | 违规装修查处案例、钢筋配置不规范 | 语义理解"楼板裂缝"=建筑质量问题 |
| **关键词搜索** | 安全生产相关规定 | 字面匹配"楼板"/"安全" |

**问题：** `混凝土强度不足怎么处理？`

| 搜索类型 | 结果 | 说明 |
|----------|------|------|
| **向量搜索** | 混凝土强度不足的处理措施 ✅ | 精确语义匹配 |
| **关键词搜索** | 混凝土强度不足的处理措施 ✅ | 精确关键词匹配 |

## 3. 代码实现

### 3.1 qa_service.py 核心逻辑

```python
class QAService:
    CATEGORY_CONFIG = {
        "qa": {
            "name": "知识问答助手",
            "system_prompt": "你是一个住建领域知识助手...",
            "search_size": 5,
        },
        "law_general": {
            "name": "执法智能助手",
            "system_prompt": "你是一个综合执法领域的智能助手...",
            "search_size": 5,
        },
        "supervise": {
            "name": "工程监管助手",
            "system_prompt": """你是一个工程监管领域的专家...""",
            "search_size": 8,  # 工程监管需要更多参考资料
        }
    }

    def ask(self, question: str, user_id: str, session_id: str, db: Session):
        # 获取对话历史
        history_messages = self.get_session_history(current_session_id, user_id, limit=5, db=db)

        # 获取类目配置
        config = self.CATEGORY_CONFIG.get(current_category, self.CATEGORY_CONFIG["qa"])
        search_size = config["search_size"]

        # 1. 检索相关知识（统一使用向量搜索）
        try:
            query_vector = embedding_service.encode_single(question)
            knowledge_ids = self.search_service.search_vector(
                query_vector=query_vector,
                user_id=user_id,
                limit=search_size
            )
            # 根据 ID 获取完整知识信息...
            search_results = {"items": items, "total": len(items)}
        except Exception as e:
            print(f"向量搜索失败，fallback 到关键词搜索: {e}")
            search_results = self.search_service.search_keyword(
                query=question, user_id=user_id, page=1, page_size=search_size
            )

        # 2. 构建 Prompt
        prompt = self._build_prompt(question, search_results['items'], history_messages, current_category)

        # 3. 调用 LLM
        if search_results['total'] > 0:
            answer = self._call_llm(prompt)
        else:
            answer = "抱歉，知识库中没有找到与您问题相关的内容..."
```

### 3.2 Embedding 服务 (embedding_service.py)

```python
class EmbeddingService:
    LOCAL_URL = "http://172.20.36.91:8001"  # WSL Embedding
    MINIMAX_URL = "https://api.minimax.chat/v1/embeddings"

    def encode_single(self, text: str) -> List[float]:
        """单个文本转为向量"""
        if self._use_local():
            return self._embed_local([text])[0]
        return self._embed_minimax([text], "query")[0]
```

### 3.3 搜索服务 (search_service.py)

```python
class SearchService:
    def search_vector(self, query_vector: List[float], user_id: str,
                     category: Optional[str] = None, limit: int = 5) -> List[str]:
        """向量搜索（Qdrant），返回知识ID列表"""
        results = self.qdrant.query_points(
            collection_name="knowledge",
            query=query_vector,
            query_filter=search_filter,
            limit=limit
        )
        return [hit.id for hit in results.points]

    def get_by_id(self, knowledge_id: str) -> Dict[str, Any]:
        """根据ID从ES获取知识详情"""
        try:
            result = self.es.get(index=self.INDEX_NAME, id=knowledge_id)
            return result['_source']
        except:
            return None
```

## 4. 三个助手差异化配置

### 4.1 System Prompt 差异化

| 助手 | Category | System Prompt |
|------|----------|---------------|
| 问答助手 | qa | 你是一个住建领域知识助手，专注于为用户提供准确、专业的政策法规和业务知识解答。 |
| 执法智能助手 | law_general | 你是一个综合执法领域的智能助手，熟悉各类执法场景，能够根据相关法律法规提供专业的执法指导和建议。 |
| 工程监管助手 | supervise | 你是一个工程监管领域的专家，专注于工程质量安全监督管理工作... |

### 4.2 search_size 差异化

| 助手 | search_size | 说明 |
|------|-------------|------|
| 问答助手 | 5 | 标准搜索量 |
| 执法智能助手 | 5 | 标准搜索量 |
| 工程监管助手 | 8 | 需更多参考资料 |

## 5. Qdrant Collection 配置

```python
collection_name = "knowledge"
vector_size = 768  # text2vec-base-chinese
distance = Distance.COSINE  # 余弦相似度

# Point 结构
PointStruct(
    id=knowledge_id,
    vector=vector,
    payload={
        "title": title,
        "content": content[:500],
        "category": category,
        "user_id": user_id  # 字符串类型
    }
)
```

## 6. 知识索引流程

### 6.1 知识创建/更新时

```
POST /knowledge/upload → index_knowledge (ES) + index_vector (Qdrant)
POST /knowledge/manual → index_knowledge (ES) + index_vector (Qdrant)
```

### 6.2 问答搜索时

```
用户问题 → Embedding → Qdrant向量搜索 → 返回知识ID列表 → ES获取详情 → 构建Prompt → LLM → 回答
```

## 7. 演示数据

### 7.1 创建的演示数据

| 标题 | 分类 |
|------|------|
| 混凝土强度不足的处理措施 | 工程质量 |
| 钢筋配置不规范的质量问题 | 工程质量 |
| 安全生产事故应急预案 | 安全生产 |
| 综合执法程序规定 | 综合执法 |
| 违法建设查处流程 | 综合执法 |
| 住宅工程质量保修制度 | 工程质量 |

### 7.2 数据状态

| 存储 | 数量 | 状态 |
|------|------|------|
| MySQL | 6条 | ✅ |
| Elasticsearch | 6条 | ✅ 已同步 |
| Qdrant | 40条 | ✅ 向量索引已建立 |

## 8. 测试验证

### 8.1 Python 测试脚本

```python
import sys
import io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding='utf-8')

from app.services.embedding_service import embedding_service
from app.services.search_service import search_service

# 测试问题
q = "楼板裂缝怎么办"

# 向量搜索
query_vector = embedding_service.encode_single(q)
vec_results = search_service.qdrant.query_points(
    collection_name='knowledge',
    query=query_vector,
    limit=3
)
print(f"向量搜索结果:")
for hit in vec_results.points:
    print(f"  - {hit.payload.get('title')}")

# 关键词搜索
kw_results = search_service.search_keyword(query=q, user_id='1', page=1, page_size=3)
print(f"关键词搜索结果:")
for item in kw_results['items']:
    print(f"  - {item['title']}")
```

### 8.2 预期结果

**向量搜索**应返回与"楼板裂缝"语义相关的知识（如"混凝土强度不足"）。

**关键词搜索**可能返回包含"楼板"或"安全"关键词的知识。

## 9. 已知限制

1. **首次加载慢**：Embedding 模型首次加载约需 10-30 秒
2. **向量维度固定**：text2vec-base-chinese 固定 768 维
3. **Qdrant 需预先配置**：collection 和向量维度需匹配
4. **Windows 终端乱码**：是显示问题，实际数据正常（用 `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')` 解决）
