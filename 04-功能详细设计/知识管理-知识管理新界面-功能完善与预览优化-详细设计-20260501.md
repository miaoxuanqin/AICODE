# 知识管理新界面 - 功能完善与预览优化

## 1. 功能概述

本次迭代对知识管理新界面（KnowledgeManageNew.vue）进行了功能完善和预览优化，主要包括：富文本编辑器集成、拖拽上传、文档在线预览、文件下载等功能。

## 2. 新增功能

### 2.1 富文本编辑器

**功能描述**：在"添加文本"功能中使用 Quill 富文本编辑器替代普通文本框

**技术实现**：
- 使用 `@vueup/vue-quill` 组件
- 主题：`snow`，工具栏：`full`
- 内容以 HTML 格式存储

**核心代码**：
```vue
<QuillEditor v-model:content="textForm.content" contentType="html" theme="snow" toolbar="full" />
```

**样式**：
```css
.editor-wrapper {
  border: 1px solid #dcdfe6;
  border-radius: 4px;
  overflow: hidden;
}
.editor-wrapper :deep(.ql-toolbar) {
  border: none;
  border-bottom: 1px solid #dcdfe6;
  background: #f5f7fa;
}
.editor-wrapper :deep(.ql-container) {
  border: none;
  font-size: 14px;
}
```

### 2.2 拖拽上传

**功能描述**：文件上传弹窗支持拖拽上传

**技术实现**：
- Element Plus Upload 组件添加 `drag` 属性
- 添加 `UploadFilled` 图标

### 2.3 文档在线预览

**功能描述**：支持 PDF 和 Word 文档在弹窗中在线预览

**技术实现**：
- 安装依赖：`mammoth`（Word）、`pdfjs-dist`（PDF）
- PDF 渲染：使用 Canvas 逐页渲染
- Word 渲染：使用 mammoth 转换为 HTML

**PDF 预览核心代码**：
```javascript
const renderPdfPreview = async (blob) => {
  const arrayBuffer = await blob.arrayBuffer()
  const pdf = await pdfjsLib.getDocument({ data: arrayBuffer }).promise
  const container = document.getElementById('pdf-preview-container')

  for (let i = 1; i <= pdf.numPages; i++) {
    const page = await pdf.getPage(i)
    const scale = 1.5
    const viewport = page.getViewport({ scale })
    const canvas = document.createElement('canvas')
    canvas.height = viewport.height
    canvas.width = viewport.width
    await page.render({ canvasContext: context, viewport }).promise
    container.appendChild(canvas)
  }
}
```

**Word 预览核心代码**：
```javascript
const renderWordPreview = async (blob) => {
  const arrayBuffer = await blob.arrayBuffer()
  const result = await mammoth.convertToHtml({ arrayBuffer })
  previewItem.value.wordContent = result.value
}
```

### 2.4 文件下载功能

**功能描述**：通过后端代理实现文件下载，解决中文文件名编码问题

**新增 API**：
- `GET /knowledge/{id}/file-url` - 获取文件访问 URL
- `GET /knowledge/{id}/download` - 后端代理下载文件

**后端实现**：
```python
@router.get("/{knowledge_id}/download")
def download_knowledge_file(knowledge_id: str, ...):
    file_data = minio_service.download_file(knowledge.file_path)
    filename = urllib.parse.quote(f"{knowledge.title}.{knowledge.file_type}")
    return Response(
        content=file_data,
        media_type=content_type,
        headers={
            'Content-Disposition': f'attachment; filename="{filename}"; filename*=UTF-8\'\'{filename}'
        }
    )
```

### 2.5 批量状态同步

**功能描述**：同步 ES/Neo4j 中的索引状态到数据库

**新增脚本**：`scripts/sync_knowledge_status.py`

**功能**：
- `sync_es_status()` - 检查 ES 中存在的文档，同步 es_indexed 和 vector_indexed 状态
- `sync_neo4j_status()` - 查询 Neo4j 中有 knowledge_id 的节点，同步 graph_indexed 状态

## 3. 修改的文件

### 后端
| 文件 | 修改内容 |
|------|---------|
| `app/api/v1/knowledge.py` | 新增 file-url、download API；优化 rebuild/clear 接口 |
| `app/services/minio_service.py` | 修复 presigned URL datetime.timedelta 参数问题 |
| `scripts/sync_knowledge_status.py` | 新增批量状态同步脚本 |

### 前端
| 文件 | 修改内容 |
|------|---------|
| `src/views/knowledge/KnowledgeManageNew.vue` | 富文本编辑器、拖拽上传、文档预览、下载功能 |
| `src/api/index.js` | 新增 download API |
| `package.json` | 新增 mammoth、pdfjs-dist 依赖 |

## 4. 依赖安装

```bash
cd code/frontend
npm install mammoth pdfjs-dist --save
```

## 5. 数据库状态

同步后状态分布：
- 全文检索 + 语义搜索 + 知识图谱 全部完成：37 条
- 全文检索 + 语义搜索完成，图谱待处理：4 条
- 其他：4 条

## 6. API 接口

### GET /knowledge/{id}/file-url
获取文件访问 URL

**响应**：
```json
{
  "url": "http://minio-url/...",
  "file_type": "docx"
}
```

### GET /knowledge/{id}/download
后端代理下载文件

**响应**：文件二进制流

**Content-Type**：
- PDF: `application/pdf`
- Word: `application/vnd.openxmlformats-officedocument.wordprocessingml.document`
- 其他: `application/octet-stream`

## 7. 待优化项

1. **PDF 预览性能**：大文件 PDF 加载慢，可考虑分页加载
2. **Word 预览样式**：Mammoth 转换的 HTML 样式需要进一步美化
3. **旧数据状态**：部分旧数据状态不一致，需要重新上传或手动重建索引
