# 工程监管助手 - 详细设计

> 创建时间：2026-04-26

## 架构设计

参考执法智能助手(LawAssistant.vue)，采用相同的技术方案：

```
SuperviseAssistant.vue → qaApi.chat() → qa_service (RAG) → ES搜索 + MiniMax LLM
```

## 会话管理

### Session Category

| category | 用途 | 页面 |
|----------|------|------|
| `qa` | 问答助手会话 | QAChat.vue |
| `law_general` | 执法助手会话 | LawAssistant.vue |
| `supervise` | 工程监管助手会话 | SuperviseAssistant.vue |

### 前端初始化流程

```
onMounted:
  1. ensureUserId() - 获取用户ID
  2. initSession():
     a. 调用 qaApi.getSessions() 获取会话列表
     b. 过滤 category='supervise' 的会话
     c. 如有已有会话，加载 loadSessionHistory()
     d. 如无已有会话，调用 qaApi.createSession('【监管】新对话', 'supervise')
```

## 前端改造

### 引入依赖

```javascript
import { qaApi, knowledgeApi } from '@/api'
```

### 状态定义

```javascript
const inputMessage = ref('')
const messages = ref([])
const isThinking = ref(false)
const chatMessagesRef = ref(null)
const currentSessionId = ref(null)

// 关联知识
const relatedKnowledge = ref([])
const isLoadingKnowledge = ref(false)

// 最近案例（复用会话历史）
const recentRecords = ref([])
```

### initSession 函数

```javascript
const initSession = async () => {
  try {
    const sessionsData = await qaApi.getSessions()
    const superviseSessions = sessionsData.items.filter(s => s.category === 'supervise')

    if (superviseSessions.length > 0) {
      currentSessionId.value = superviseSessions[0].id
      await loadSessionHistory()
    } else {
      const data = await qaApi.createSession('【监管】新对话', 'supervise')
      currentSessionId.value = data.id
    }
  } catch (error) {
    console.error('初始化监管会话失败:', error)
  }
}
```

### loadSessionHistory 函数

```javascript
const loadSessionHistory = async () => {
  if (!currentSessionId.value) return

  try {
    const data = await qaApi.getSession(currentSessionId.value)
    if (data && data.messages) {
      messages.value = data.messages.map(m => ({
        role: m.role,
        content: m.content,
        time: m.time
      }))
    }

    // 加载关联知识
    const lastUserMsg = messages.value.filter(m => m.role === 'user').slice(-1)[0]
    if (lastUserMsg) {
      await loadRelatedKnowledge(lastUserMsg.content)
    }
  } catch (error) {
    console.error('加载会话历史失败:', error)
  }
}
```

### loadRelatedKnowledge 函数

```javascript
const loadRelatedKnowledge = async (query) => {
  if (!query) return

  isLoadingKnowledge.value = true
  try {
    const result = await knowledgeApi.search({ q: query, page: 1, page_size: 5 })
    relatedKnowledge.value = (result.items || []).map(item => ({
      id: item.id,
      type: item.category,
      typeName: getTypeName(item.category),
      title: item.title
    }))
  } catch (error) {
    relatedKnowledge.value = []
  } finally {
    isLoadingKnowledge.value = false
  }
}
```

### handleSend 函数

```javascript
const handleSend = async () => {
  if (!inputMessage.value.trim()) return

  const userMsg = {
    role: 'user',
    content: inputMessage.value,
    time: new Date().toLocaleTimeString()
  }
  messages.value.push(userMsg)
  inputMessage.value = ''
  isThinking.value = true

  try {
    const userId = await ensureUserId()
    const result = await qaApi.chat(userMsg.content, userId, currentSessionId.value)

    const aiMsg = {
      role: 'assistant',
      content: result.answer,
      cards: result.cards || [],
      time: new Date().toLocaleTimeString()
    }
    messages.value.push(aiMsg)

    if (result.session_id && result.session_id !== currentSessionId.value) {
      currentSessionId.value = result.session_id
    }

    // 动态加载关联知识
    await loadRelatedKnowledge(userMsg.content)

  } catch (error) {
    ElMessage.error('问答服务出错，请稍后重试')
  } finally {
    isThinking.value = false
  }

  scrollToBottom()
}
```

## 页面布局

左侧(16列)：对话区域 + 输入框
右侧(8列)：
- 常见问题类型卡片
- 关联规范列表（动态加载）
- 最近监管记录（复用会话历史）

## 改造文件

| 文件 | 变更 |
|------|------|
| `SuperviseAssistant.vue` | 对接 qaApi，移除 mock 数据 |

## Markdown 渲染

复用 marked 库渲染助手回答。

## 测试验证

```bash
# 创建监管会话
POST /api/v1/qa/session
Body: {"title": "【监管】测试", "category": "supervise"}

# 发送消息
POST /api/v1/qa/chat
Body: {"question": "混凝土强度不足怎么处理", "user_id": "1", "session_id": "..."}
```
