# 知识管理-知识管理新界面-分类树功能开发-详细设计

**日期**: 2026-05-03
**模块**: 知识管理
**菜单**: 知识管理新界面
**功能点**: 分类树形结构与管理

---

## 一、需求概述

根据初设要求，知识管理需要支持多层级分类体系。原来 `Knowledge.category` 是固定4个一级分类的字符串枚举值（law/tech/case/policy），无法满足多层级分类和分类管理的需求。

本次实现：在知识管理页面左侧添加分类树形结构，并提供分类的增删改管理功能。

---

## 二、数据库设计

### 2.1 现有 categories 表结构

现有 `categories` 表已存在，结构如下：
```sql
categories (
  id          INT AUTO_INCREMENT PRIMARY KEY,
  name        VARCHAR(100) NOT NULL,
  parent_id   INT NULL,
  level       INT DEFAULT 1,
  description TEXT,
  created_at  DATETIME DEFAULT CURRENT_TIMESTAMP
)
```

### 2.2 新增字段

为支持排序和自动更新时间，新增字段：
```sql
ALTER TABLE categories ADD COLUMN sort_order INT DEFAULT 0;
ALTER TABLE categories ADD COLUMN updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;
```

### 2.3 初始化数据

现有8个一级分类：
- id=1: 建筑市场
- id=2: 质量安全
- id=3: 综合执法
- id=4: 住房管理
- id=5: 城乡规划
- id=6: 城市规划
- id=7: 招标投标
- id=8: 工程变更

---

## 三、后端设计

### 3.1 Category 模型

**文件**: `code/backend/app/models/category.py`

```python
class Category(Base):
    __tablename__ = "categories"

    id = Column(Integer, primary_key=True, autoincrement=True)
    name = Column(String(100), nullable=False)
    parent_id = Column(Integer, ForeignKey("categories.id"), nullable=True, index=True)
    level = Column(Integer, default=1)
    description = Column(Text, nullable=True)
    sort_order = Column(Integer, default=0)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

    children = relationship("Category", backref="parent", remote_side=[id], foreign_keys=[parent_id])
```

### 3.2 Category Schema

**文件**: `code/backend/app/schemas/category.py`

```python
class CategoryCreate(BaseModel):
    name: str = Field(..., min_length=1, max_length=100)
    parent_id: Optional[str] = None  # 前端传字符串，后端转int
    level: int = 1
    description: Optional[str] = None
    sort_order: int = 0

class CategoryUpdate(BaseModel):
    name: Optional[str] = Field(None, min_length=1, max_length=100)
    parent_id: Optional[str] = None
    level: Optional[int] = None
    description: Optional[str] = None
    sort_order: Optional[int] = None

class CategoryResponse(BaseModel):
    id: int
    name: str
    parent_id: Optional[int] = None
    level: int
    description: Optional[str] = None
    sort_order: int
    children: List["CategoryResponse"] = []

    class Config:
        from_attributes = True
```

### 3.3 Category API

**文件**: `code/backend/app/api/v1/category.py`

| 接口 | 方法 | 说明 |
|------|------|------|
| `/categories` | GET | 获取分类树形列表（返回嵌套树结构） |
| `/categories/{id}` | GET | 获取单个分类 |
| `/categories` | POST | 创建分类 |
| `/categories/{id}` | PUT | 更新分类 |
| `/categories/{id}` | DELETE | 删除分类（仅管理员，需无子分类和知识关联） |

**树形结构构建**:
```python
def build_tree(categories, parent_id=None):
    tree = []
    for cat in categories:
        if (parent_id is None and cat.parent_id is None) or \
           (cat.parent_id is not None and str(cat.parent_id) == str(parent_id)):
            node = {
                "id": cat.id,
                "name": cat.name,
                "parent_id": cat.parent_id,
                "level": cat.level,
                "sort_order": cat.sort_order,
                "children": build_tree(categories, cat.id)
            }
            tree.append(node)
    tree.sort(key=lambda x: x.get("sort_order", 0))
    return tree
```

### 3.4 路由注册

**文件**: `code/backend/app/api/v1/__init__.py`

```python
from app.api.v1 import auth, users, roles, knowledge, qa, graph, category

api_router.include_router(category.router)
```

---

## 四、前端设计

### 4.1 分类 API

**文件**: `code/frontend/src/api/index.js`

```javascript
export const categoryApi = {
  list: () => request.get('/categories'),
  create: (data) => request.post('/categories', data),
  update: (id, data) => request.put(`/categories/${id}`, data),
  delete: (id) => request.delete(`/categories/${id}`),
}
```

### 4.2 页面布局

**文件**: `code/frontend/src/views/knowledge/KnowledgeManageNew.vue`

采用左右两栏布局：
- 左侧 `category-sidebar` (220px)：分类树
- 右侧 `main-content`：现有内容区域

```html
<div class="knowledge-manage-new">
  <div class="category-sidebar">
    <div class="sidebar-header">
      <span>知识分类</span>
      <el-button size="small" link type="primary" @click="showCategoryDialog('create')">+ 新增</el-button>
    </div>
    <el-tree
      ref="categoryTreeRef"
      :data="categoryTree"
      :props="{ children: 'children', label: 'name' }"
      node-key="id"
      default-expand-all
      highlight-current
      :expand-on-click-node="false"
      @node-click="handleCategoryNodeClick"
    >
      <template #default="{ node, data }">
        <span class="tree-node">
          <span v-if="data.id === ''" class="node-icon">📁</span>
          <span v-else-if="data.children && data.children.length > 0" class="node-icon">📂</span>
          <span v-else class="node-icon">📄</span>
          <span class="node-label">{{ node.label }}</span>
          <span class="node-actions">
            <el-button size="small" link type="primary" @click.stop="showCategoryDialog('edit', data)">编辑</el-button>
            <el-button size="small" link type="danger" @click.stop="handleDeleteCategory(data)">删除</el-button>
          </span>
        </span>
      </template>
    </el-tree>
  </div>

  <div class="main-content">
    <!-- 页面标题栏 -->
    <!-- 统计卡片 -->
    <!-- 筛选栏 -->
    <!-- 知识列表/网格 -->
    <!-- 分页 -->
  </div>
</div>
```

### 4.3 分类管理弹窗

```vue
<el-dialog v-model="showCategoryDialogVisible" :title="categoryDialogTitle" width="400px">
  <el-form ref="categoryFormRef" :model="categoryForm" label-width="80px">
    <el-form-item label="分类名称" required>
      <el-input v-model="categoryForm.name" placeholder="请输入分类名称" />
    </el-form-item>
    <el-form-item label="上级分类">
      <el-select v-model="categoryForm.parent_id" placeholder="无上级分类（顶级）" clearable>
        <el-option v-for="cat in flatCategoryList" :key="cat.id" :label="cat.name" :value="cat.id" />
      </el-select>
    </el-form-item>
    <el-form-item label="排序">
      <el-input-number v-model="categoryForm.sort_order" :min="0" />
    </el-form-item>
  </el-form>
  <template #footer>
    <el-button @click="showCategoryDialogVisible = false">取消</el-button>
    <el-button type="primary" @click="handleSaveCategory">确定</el-button>
  </template>
</el-dialog>
```

### 4.4 关键函数

| 函数 | 说明 |
|------|------|
| `loadCategoryTree()` | 调用 categoryApi.list() 获取树形数据 |
| `handleCategoryNodeClick(data)` | 点击分类节点，设置 `filters.category = String(data.id)` 并调用 `handleFilter()` |
| `showCategoryDialog(action, data)` | 显示新增/编辑分类弹窗 |
| `handleSaveCategory()` | 判断 action 执行新建或更新 |
| `handleDeleteCategory(data)` | 删除分类（需确认） |

### 4.5 扁平分类列表

用于上级分类选择器，将树结构展平：
```javascript
const flatCategoryList = computed(() => {
  const flatten = (cats, result = []) => {
    cats.forEach(cat => {
      result.push(cat)
      if (cat.children && cat.children.length > 0) {
        flatten(cat.children, result)
      }
    })
    return result
  }
  return flatten(categoryTree.value)
})
```

---

## 五、用户交互流程

### 5.1 分类树点击筛选

1. 用户点击分类树节点
2. `handleCategoryNodeClick(data)` 被调用
3. 设置 `filters.category = String(data.id)`
4. 调用 `handleFilter()` → `loadData()`
5. 后端根据 category 参数筛选知识

### 5.2 新增分类

1. 用户点击"+ 新增"按钮
2. 弹窗显示，表单默认 empty
3. 用户填写分类名称、选择上级分类、设置排序
4. 点击确定，调用 `categoryApi.create()`
5. 成功后关闭弹窗，调用 `loadCategoryTree()` 刷新树

### 5.3 编辑分类

1. 用户 hover 树节点，显示编辑/删除按钮
2. 点击"编辑"，弹窗显示当前分类数据
3. 用户修改后点击确定，调用 `categoryApi.update()`
4. 成功后刷新树

### 5.4 删除分类

1. 用户点击"删除"，弹出确认框
2. 确认后调用 `categoryApi.delete()`
3. 成功后刷新树

---

## 六、相关文件清单

| 操作 | 文件路径 |
|------|---------|
| 新建 | `code/backend/app/models/category.py` |
| 新建 | `code/backend/app/api/v1/category.py` |
| 新建 | `code/backend/app/schemas/category.py` |
| 修改 | `code/backend/app/api/v1/__init__.py` |
| 修改 | `code/frontend/src/api/index.js` — 新增 categoryApi |
| 修改 | `code/frontend/src/views/knowledge/KnowledgeManageNew.vue` — 分类树 + 布局修复 |

---

## 七、已知问题与限制

1. **分类与知识关联**：目前 `Knowledge.category` 仍使用字符串（law/tech/case/policy），尚未迁移到 `category_id` 外键
2. **分类管理权限**：删除分类只检查知识关联，未检查用户权限
3. **分类图标**：树形节点使用 emoji 图标区分层级，未来可改为 SVG 图标