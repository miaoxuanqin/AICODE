# 开发环境配置

本文档用于说明项目开发前需要准备的基础环境、本机配置、联调资源和常用初始化命令。

## WSL虚拟机
版本：Ubuntu 24.04.4 LTS
IP：172.20.36.91
用户名：mm
密码：mm

## 中间件服务（Docker运行）

| 服务 | 地址 | 版本 | 状态 |
|------|------|------|------|
| MySQL | 172.20.36.91:3306 | 8.0.45 | 已有 |
| Elasticsearch | 172.20.36.91:9200 | 8.13.0 | 已安装 |
| Redis | 172.20.36.91:6379 | 7-alpine | 已安装 |
| Qdrant | 172.20.36.91:6333 | 1.17.1 | 已安装 |
| Neo4j | 172.20.36.91:7474 | latest | **2026-04-27 新增** |

## Embedding 服务（WSL运行）

| 服务 | 地址 | 模型 | 状态 |
|------|------|------|------|
| Embedding | 172.20.36.91:8001 | text2vec-base-chinese | 已部署 |

### Embedding 服务启动命令

Embedding 服务已配置为 **cron @reboot 自动启动**，无需手动启动。

```bash
# 手动启动（如需要）
wsl -d Ubuntu -e bash -c "cd /home/mm/embedding && python3 embedding_service.py &"

# 验证服务
curl http://172.20.36.91:8001/health
```

### 停止/重启服务

```bash
# 停止
pkill -f "python3.*embedding_service"

# 重启（如文件更新）
pkill -f "python3.*embedding_service" && sleep 2 && wsl -d Ubuntu -e bash -c "cd /home/mm/embedding && python3 embedding_service.py &"
```

### Embedding 服务接口
```bash
# 健康检查
GET http://172.20.36.91:8001/health

# 文本向量
POST http://172.20.36.91:8001/embed
Body: ["文本"]
Response: {"embeddings": [[0.1, 0.2, ...]]}
```

### WSL Embedding 服务部署
```bash
# WSL 中安装依赖
pip3 install fastapi uvicorn sentence-transformers

# 启动服务
python3 embedding_service.py
```

### Docker 容器自动启动

所有中间件容器已配置 `unless-stopped` 重启策略，WSL 重启后会自动启动：

```bash
# 已为所有容器配置（无需重复执行）
docker update --restart unless-stopped redis qdrant elasticsearch neo4j minio
```

> 注意：只有通过 `docker stop` 手动停止的容器才会在重启后保持停止状态。

### Docker启动命令

自动启动后，通常无需手动启动。如需手动操作：

```bash
# 启动所有中间件
docker start redis qdrant elasticsearch neo4j minio

# 单独启动
docker start redis
docker start qdrant
docker start elasticsearch
docker start neo4j
docker start minio
```

## Neo4j 图数据库（2026-04-27 新增）

### 自动启动配置

Neo4j 容器已配置为随 WSL 自动启动：

```bash
# 已执行（无需重复执行）
docker update --restart unless-stopped neo4j

# 验证
docker inspect neo4j --format '{{.HostConfig.RestartPolicy.Name}}'
# 输出：unless-stopped
```

重启后容器会自动启动，无需手动执行 `docker start neo4j`。

### 连接信息

| 配置 | 值 |
|------|-----|
| HTTP URL | http://172.20.36.91:7474 |
| Bolt URL | bolt://172.20.36.91:7687 |
| 用户名 | neo4j（默认） |
| 密码 | neo4j（默认） |
| 认证 | 已禁用 |

### Docker 容器

```bash
# 容器名称
neo4j

# 端口映射
-p 7474:7474 (HTTP)
-p 7687:7687 (Bolt)

# 数据卷
~/neo4j/data
~/neo4j/logs
~/neo4j/plugins
```

### Neo4j 部署命令

```bash
# 拉取镜像
docker pull neo4j:latest

# 启动容器（已执行）
docker run -d \
  --name neo4j \
  -p 7474:7474 \
  -p 7687:7687 \
  -v ~/neo4j/data:/data \
  -v ~/neo4j/logs:/logs \
  -v ~/neo4j/plugins:/plugins \
  -e NEO4J_AUTH=none \
  neo4j:latest
  
```

### Neo4j 管理界面

打开浏览器访问：http://172.20.36.91:7474

可使用 Cypher 查询图数据：

```cypher
// 查看所有节点
MATCH (n) RETURN n

// 查看所有关系
MATCH ()-[r]->() RETURN r

// 删除所有数据
MATCH (n) DETACH DELETE n
```

## 快速清单

## 1. 基础环境

| 软件 | 版本 | 安装方式 |
|------|------|---------|
| Python | 3.10+ | 本地已有 |
| Node.js | 18+ | 本地已有 |
| Docker | 最新 | WSL中安装 |
| Git | - | WSL中已有 |

## 2. IDE 与工具推荐

- **后端**：VS Code / PyCharm
- **前端**：VS Code / WebStorm
- **数据库**：DBeaver / Navicat
- **API测试**：Postman / Apifox

## 3. 数据库连接信息

数据库版本：mysql Ver 8.0.45-0ubuntu0.24.04.1 for Linux on x86_64 ((Ubuntu))

### 连接参数

- IP：172.20.36.91
- Port：3306
- Database：`mydatabase`
- Username：`myuser`
- Password：`1`

### 数据库说明

### 测试账号

- 用户名：`admin`
- 密码：`admin@123`

## 4. 中间件连接信息

### Elasticsearch
- 地址：http://172.20.36.91:9200
- 无认证

### Redis
- 地址：172.20.36.91:6379
- 无密码

### Qdrant
- 地址：http://172.20.36.91:6333
- 无认证

## 5. 应用编码信息

### 5.1 用户、角色、权限体系

系统采用 RBAC 模型，支持多级组织架构。

#### 预置角色

| 角色标识 | 角色名称 | 说明 |
|---------|---------|------|
| platform_admin | 平台管理员 | 系统最高权限，拥有所有功能权限 |
| provincial_admin | 省厅管理员 | 省级知识管理权限 |
| city_admin | 市县管理员 | 市县级知识管理权限 |
| business_user | 业务用户 | 普通业务人员权限 |
| viewer | 访客 | 只读权限 |

#### 权限模块

| 模块 | 说明 |
|-----|------|
| system | 系统管理（用户、角色、权限） |
| knowledge | 知识管理（搜索、查看、创建、审批） |
| portal | 门户配置 |

#### 超级管理员

- 用户名：`admin`
- 密码：`admin123`
- 角色：`platform_admin`
- 组织：`海南省住房和城乡建设厅`

### 5.2 API 路由

| 接口 | 方法 | 说明 | 权限 |
|-----|------|------|------|
| /api/v1/auth/login | POST | 用户登录 | 公开 |
| /api/v1/auth/logout | POST | 用户登出 | 需登录 |
| /api/v1/auth/me | GET | 当前用户信息 | 需登录 |
| /api/v1/users | GET | 用户列表 | system:user:list |
| /api/v1/users/{id} | GET | 用户详情 | system:user:read |
| /api/v1/users | POST | 创建用户 | system:user:create |
| /api/v1/users/{id} | PATCH | 更新用户 | system:user:update |
| /api/v1/users/{id} | DELETE | 删除用户 | system:user:delete |
| /api/v1/roles | GET | 角色列表 | system:role:list |
| /api/v1/permissions | GET | 权限列表 | system:permission:list |

### 5.3 数据权限控制

系统支持多维度数据权限控制：

- **模块权限**：用户能否使用某功能
- **数据权限**：用户能访问哪些知识（按组织、分类、密级）
- **组织范围**：用户在哪个地域范围内有效

## 6. 工作流定义信息

## 7. 调度能力

使用 Celery + Celery Beat 进行定时任务调度：
- P0采集：每日增量采集（国家法规、国标）
- P1采集：每周全量采集（省级法规、行业标准）

## 8. 文件服务能力

### MinIO (S3兼容对象存储)
- 地址：http://172.20.36.91:9000
- Console 地址：http://172.20.36.91:9001
- 用户名：`minioadmin`
- 密码：`minioadmin123`
- 数据目录：`~/minio/data`
- 状态：已安装

#### Docker 命令
```bash
# 启动 MinIO
docker start minio

# 停止 MinIO
docker stop minio

# 重启 MinIO
docker restart minio
```

#### 连接示例 (Python)
```python
from minio import Minio

client = Minio(
    "172.20.36.91:9000",
    access_key="minioadmin",
    secret_key="minioadmin123",
    secure=False
)
```

## 9. 常见初始化命令

### Git 仓库初始化
```bash
git init
```

### 后端依赖安装
```bash
cd code/backend
pip install -r requirements.txt
```

### 前端依赖安装
```bash
cd code/frontend
npm install
```

### 启动后端服务
```bash
cd code/backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 启动前端服务
```bash
cd code/frontend
npm run dev
```

### 数据库初始化
```bash
# 初始化RBAC表结构和数据
mysql -h 172.20.36.91 -u myuser -p1 mydatabase < code/sql/init_rbac.sql
```

## 快速启动清单

WSL 重启后自动启动：
- Embedding 服务（cron @reboot）
- 所有中间件容器（Docker restart policy，除非手动 stop）

### 中间件服务（Docker）

已配置自动启动，通常无需手动启动。如需手动：
```bash
wsl -d Ubuntu -e bash -c "docker start redis qdrant elasticsearch neo4j minio"
```

### 后端服务
```bash
cd code/backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 前端服务
```bash
cd code/frontend && npm run dev
```

## 10. 环境变量配置

创建 `code/backend/.env` 文件：
```bash
DATABASE_URL=mysql://myuser:1@172.20.36.91:3306/mydatabase
REDIS_URL=redis://172.20.36.91:6379
ELASTICSEARCH_URL=http://172.20.36.91:9200
QDRANT_URL=http://172.20.36.91:6333
JWT_SECRET=your-secret-key-change-in-production
LOG_LEVEL=INFO
```

## 11. 快速启动脚本

项目启动顺序：后端 → 前端

### Windows 启动脚本 (code/start-dev.bat)
```batch
@echo off
chcp 65001 >nul
echo ====================================
echo 知识库系统开发环境启动
echo ====================================

echo.
echo [1/2] 启动后端服务 (端口 8001)...
cd /d %~dp0code\backend
start "Backend" cmd /c "python -m uvicorn app.main:app --host 0.0.0.0 --port 8001"

echo 等待后端启动...
timeout /t 3 /nobreak >nul

echo.
echo [2/2] 启动前端服务 (端口 3000)...
cd /d %~dp0code\frontend
start "Frontend" cmd /c "npm run dev"

echo.
echo ====================================
echo 启动完成！
echo 前端: http://localhost:3000
echo 后端: http://localhost:8001
echo API文档: http://localhost:8001/docs
echo ====================================
pause
```

### Linux/Mac 启动脚本 (code/start-dev.sh)
```bash
#!/bin/bash
echo "===================================="
echo "知识库系统开发环境启动"
echo "===================================="

echo ""
echo "[1/2] 启动后端服务 (端口 8001)..."
cd "$(dirname "$0")/code/backend"
python -m uvicorn app.main:app --host 0.0.0.0 --port 8001 &
BACKEND_PID=$!

echo "等待后端启动..."
sleep 3

echo ""
echo "[2/2] 启动前端服务 (端口 3000)..."
cd "$(dirname "$0")/code/frontend"
npm run dev &
FRONTEND_PID=$!

echo ""
echo "===================================="
echo "启动完成！"
echo "前端: http://localhost:3000"
echo "后端: http://localhost:8001"
echo "API文档: http://localhost:8001/docs"
echo "===================================="
echo ""
echo "按 Ctrl+C 停止所有服务"
trap "kill $BACKEND_PID $FRONTEND_PID 2>/dev/null" EXIT
wait
```

### 手动启动命令

如果脚本不可用，手动按顺序执行：

```bash
# 1. 启动后端
cd code/backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8001

# 2. 新开终端，启动前端
cd code/frontend
npm run dev
```

### 停止服务

```bash
# Windows
taskkill /F /FI "WINDOWTITLE eq Backend*" 2>nul
taskkill /F /FI "WINDOWTITLE eq Frontend*" 2>nul

# 或直接杀掉进程
netstat -ano | findstr "8001"  # 找到 PID
taskkill /F /PID <PID>
netstat -ano | findstr "3000"  # 找到 PID
taskkill /F /PID <PID>

# Linux/Mac
pkill -f "uvicorn.*app.main"
pkill -f "vite"
```
