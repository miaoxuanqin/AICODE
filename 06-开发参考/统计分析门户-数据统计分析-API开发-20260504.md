# 统计分析门户-API开发

> 文档类型：开发参考
> 创建日期：2026-05-04
> 最后更新：2026-05-04

---

## 一、API 接口

### 1.1 门户统计接口

**接口路径**：`GET /api/v1/knowledge/stats/portal`

**认证**：需要登录

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| date_from | string | 否 | 开始日期 (YYYY-MM-DD) |
| date_to | string | 否 | 结束日期 (YYYY-MM-DD) |

**响应示例**：
```json
{
  "total": 43,
  "monthly_new": 2,
  "es_indexed": 39,
  "vector_indexed": 39,
  "graph_nodes": 42,
  "user_count": 7,
  "categories": [
    {"category": "law", "category_name": "法律法规", "count": 11}
  ],
  "tags": [
    {"tag": "施工安全", "count": 5}
  ],
  "sources": [
    {"source": "国家法规", "count": 10}
  ],
  "trend": [
    {"date": "2026-05-01", "new_count": 2, "index_count": 1}
  ],
  "index_status": [
    {"category": "law", "category_name": "法律法规", "completed": 10, "in_progress": 1, "pending": 0, "failed": 0}
  ]
}
```

---

## 二、后端实现

### 2.1 Schema 定义

**文件**：`code/backend/app/schemas/knowledge.py`

```python
class CategoryStatsItem(BaseModel):
    category: str
    category_name: str
    count: int

class TagStatsItem(BaseModel):
    tag: str
    count: int

class SourceStatsItem(BaseModel):
    source: str
    count: int

class TrendStatsItem(BaseModel):
    date: str
    new_count: int
    index_count: int

class IndexStatusItem(BaseModel):
    category: str
    category_name: str
    completed: int
    in_progress: int
    pending: int
    failed: int

class PortalStatsResponse(BaseModel):
    total: int = 0
    monthly_new: int = 0
    es_indexed: int = 0
    vector_indexed: int = 0
    graph_nodes: int = 0
    user_count: int = 0
    categories: List[CategoryStatsItem] = []
    tags: List[TagStatsItem] = []
    sources: List[SourceStatsItem] = []
    trend: List[TrendStatsItem] = []
    index_status: List[IndexStatusItem] = []
```

### 2.2 API 实现

**文件**：`code/backend/app/api/v1/knowledge.py`

```python
@router.get("/stats/portal", response_model=PortalStatsResponse)
def get_portal_stats(
    date_from: Optional[str] = Query(None),
    date_to: Optional[str] = Query(None),
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """获取门户统计数据"""
    # ... 实现代码
```

---

## 三、前端调用

### 3.1 API 方法

**文件**：`code/frontend/src/api/index.js`

```javascript
export const knowledgeApi = {
  // ...
  portalStats: (params) => request.get('/knowledge/stats/portal', { params }),
}
```

### 3.2 Vue 组件调用

**文件**：`code/frontend/src/views/portal/PortalPage.vue`

```javascript
import { knowledgeApi } from '@/api'

const loadPortalStats = async () => {
  try {
    const params = {}
    if (dateRange.value && dateRange.value.length === 2) {
      params.date_from = dateRange.value[0]
      params.date_to = dateRange.value[1]
    }
    const res = await knowledgeApi.portalStats(params)
    portalData.value = res
    updateStatsCards()
    updateCharts()
  } catch (error) {
    console.error('加载门户数据失败', error)
  }
}
```

---

## 四、已知问题

### 4.1 MySQL JSON 字段聚合

**问题**：`tags` 是 JSON 类型字段，直接使用 SQL GROUP BY 会报错：
```
In aggregated query without GROUP BY, expression #1 of SELECT list contains nonaggregated column 'mydatabase.knowledge.tags'
```

**解决方案**：在 Python 中手动遍历统计：
```python
tag_count_map = {}
tag_query = db.query(Knowledge.tags).filter(
    Knowledge.tags.isnot(None),
    Knowledge.user_id == str(current_user.id) if not is_admin else True
).all()
for row in tag_query:
    if row.tags:
        for tag in row.tags:
            tag_count_map[tag] = tag_count_map.get(tag, 0) + 1
```

---

## 五、验证方法

### 5.1 API 验证

```bash
# 登录获取 token
TOKEN=$(curl -s -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' | jq -r '.access_token')

# 调用 API
curl -s http://localhost:8000/api/v1/knowledge/stats/portal \
  -H "Authorization: Bearer $TOKEN" | jq
```

### 5.2 前端验证

访问：http://localhost:3000/portal

检查：
1. 统计卡片显示真实数据
2. 图表渲染正常
3. 日期筛选生效
4. 刷新按钮正常

---

## 六、相关文件

| 文件 | 说明 |
|------|------|
| `code/backend/app/schemas/knowledge.py` | PortalStatsResponse 等 Schema |
| `code/backend/app/api/v1/knowledge.py` | /stats/portal 接口 |
| `code/frontend/src/api/index.js` | portalStats API 方法 |
| `code/frontend/src/views/portal/PortalPage.vue` | 统计分析门户页面 |
