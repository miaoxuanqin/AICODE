# 知识模块-图谱增强RAG-开发参考

> 创建日期：2026-04-27
> 更新日期：2026-04-27

---

## 一、功能概述

图谱增强问答（Graph-RAG）基于知识库实现多跳推理问答，支持实体识别、知识检索、图谱构建、回答生成。

**当前实现**：基于知识库（ES + Qdrant）实现图谱增强 RAG，未来可扩展 Neo4j 图数据库。

---

## 二、文件清单

### 后端文件

| 文件 | 说明 |
|------|------|
| `code/backend/app/services/graph_service.py` | 图谱服务类 |
| `code/backend/app/api/v1/graph.py` | Graph API 路由 |
| `code/backend/app/api/v1/__init__.py` | 路由注册（已更新） |

### 前端文件

| 文件 | 说明 |
|------|------|
| `code/frontend/src/views/graph/GraphQAChat.vue` | 图谱问答页面 |
| `code/frontend/src/api/index.js` | graphApi（已添加） |

---

## 三、后端实现

### 3.1 graph_service.py

**路径**：`code/backend/app/services/graph_service.py`

```python
"""
知识图谱服务 (Graph Service)
基于知识库构建实体关系图谱，支持多跳推理问答
"""
import uuid
import re
from typing import List, Optional, Dict, Any, Tuple

import anthropic
from app.config import get_settings
from app.services.search_service import search_service
from app.services.embedding_service import embedding_service

settings = get_settings()


class GraphService:
    """知识图谱服务"""

    def __init__(self):
        self._llm_client = None
        self.entity_patterns = {
            "law": ["条例", "法规", "法律", "办法", "规定", "标准", "规范"],
            "case": ["事故", "案例", "案件", "处罚", "违法"],
            "policy": ["通知", "意见", "决定", "公告", "政策"],
            "article": [r"第\d+条", r"第\d+款", r"第\d+项"],
            "penalty": ["罚款", "处罚", "刑事责任", "行政处分"],
            "standard": ["标准", "规范", "规程", "指标"]
        }

    def extract_entities(self, text: str) -> List[Dict[str, str]]:
        """从文本中提取实体和类型"""
        entities = []

        # 使用规则匹配已知实体类型
        for entity_type, keywords in self.entity_patterns.items():
            for keyword in keywords:
                if keyword in text:
                    pattern = rf'{keyword}[^，。！？、\n]{{0,30}}'
                    matches = re.findall(pattern, text)
                    for match in matches:
                        if match and len(match) > len(keyword):
                            entities.append({
                                "text": match.strip(),
                                "type": entity_type
                            })

        # 使用 LLM 辅助提取关键实体
        try:
            llm_entities = self._extract_entities_with_llm(text)
            for ent in llm_entities:
                if ent["text"] not in [e["text"] for e in entities]:
                    entities.append(ent)
        except Exception as e:
            print(f"LLM 实体提取失败: {e}")

        return entities[:10]

    def reason(self, question: str, user_id: str = None, db=None) -> Dict[str, Any]:
        """
        执行图谱增强推理问答
        """
        reasoning_chain = []
        all_search_items = []

        # Step 1: 实体识别
        step1_entities = self.extract_entities(question)
        reasoning_chain.append({
            "step": 1,
            "query": "理解问题并识别关键实体",
            "result": f"识别到 {len(step1_entities)} 个关键实体",
            "entities": [e["text"] for e in step1_entities[:5]],
            "details": {
                "search_query": question,
                "knowledge_found": []
            }
        })

        # Step 2: 首次搜索
        try:
            query_vector = embedding_service.encode_single(question)
            knowledge_ids = search_service.search_vector(
                query_vector=query_vector,
                user_id=user_id or "anonymous",
                limit=6
            )
            items = []
            for kid in knowledge_ids:
                doc = search_service.get_by_id(kid)
                if doc:
                    items.append(doc)
            first_results = {"items": items, "total": len(items)}
        except Exception as e:
            first_results = search_service.search_keyword(
                query=question,
                user_id=user_id or "anonymous",
                page=1,
                page_size=6
            )

        reasoning_chain.append({
            "step": 2,
            "query": "检索相关知识库内容",
            "result": f"找到 {first_results['total']} 条相关知识",
            "entities": [],
            "details": {
                "search_query": question,
                "knowledge_found": [item.get("title", "") for item in first_results.get("items", [])]
            }
        })

        all_search_items.extend(first_results.get("items", []))

        # Step 3: 关系扩展
        expand_query = self._generate_expand_query(question, first_results.get("items", []))
        if expand_query and expand_query != question:
            try:
                query_vector = embedding_service.encode_single(expand_query)
                knowledge_ids = search_service.search_vector(
                    query_vector=query_vector,
                    user_id=user_id or "anonymous",
                    limit=4
                )
                items = []
                for kid in knowledge_ids:
                    doc = search_service.get_by_id(kid)
                    if doc:
                        items.append(doc)
                second_results = {"items": items, "total": len(items)}
            except Exception:
                second_results = search_service.search_keyword(
                    query=expand_query,
                    user_id=user_id or "anonymous",
                    page=1,
                    page_size=4
                )

            reasoning_chain.append({
                "step": 3,
                "query": "扩展关联知识",
                "result": f"补充找到 {second_results['total']} 条关联知识",
                "entities": [],
                "details": {
                    "search_query": expand_query,
                    "knowledge_found": [item.get("title", "") for item in second_results.get("items", [])]
                }
            })
            all_search_items.extend(second_results.get("items", []))
        else:
            reasoning_chain.append({
                "step": 3,
                "query": "扩展关联知识",
                "result": "无需扩展",
                "entities": [],
                "details": {
                    "search_query": "",
                    "knowledge_found": []
                }
            })

        # Step 4: 去重
        seen_ids = set()
        unique_items = []
        for item in all_search_items:
            if item.get("id") not in seen_ids:
                seen_ids.add(item.get("id"))
                unique_items.append(item)

        reasoning_chain.append({
            "step": 4,
            "query": "整合知识并生成回答",
            "result": f"整合 {len(unique_items)} 条知识进行回答",
            "entities": [],
            "details": {
                "search_query": "",
                "knowledge_found": [item.get("title", "") for item in unique_items]
            }
        })

        # Step 5: 构建图谱
        graph_entities = step1_entities if step1_entities else [{"text": question[:20], "type": "question"}]
        nodes, edges = self.build_graph(graph_entities, unique_items[:6])
        graph_data = {"nodes": nodes, "edges": edges}

        # Step 6: 生成回答
        final_answer = self._generate_answer(question, unique_items) if unique_items else "抱歉，知识库中没有找到相关内容。"

        # Step 7: 构建引用
        citations = []
        type_names = {"law": "法规", "case": "案例", "policy": "政策", "tech": "技术", "article": "条款"}
        for item in unique_items[:5]:
            category = item.get("category", "tech")
            citations.append({
                "id": item.get("id", ""),
                "type": category,
                "typeName": type_names.get(category, "知识"),
                "title": item.get("title", "")
            })

        return {
            "answer": final_answer,
            "reasoning_chain": reasoning_chain,
            "graph_data": graph_data,
            "citations": citations
        }
```

### 3.2 graph.py API

**路径**：`code/backend/app/api/v1/graph.py`

```python
"""
图谱增强问答 API
基于知识图谱的多跳推理问答
"""
from fastapi import APIRouter, Depends, Query
from pydantic import BaseModel
from typing import List, Optional, Dict, Any

from app.services.graph_service import graph_service
from core.rbac import get_current_user

router = APIRouter(prefix="/graph", tags=["图谱问答"])


class GraphQARequest(BaseModel):
    question: str
    session_id: Optional[str] = None


class ReasoningStep(BaseModel):
    step: int
    query: str
    result: str
    entities: Optional[List[str]] = []
    details: Optional[Dict[str, Any]] = {}  # 新增：搜索详情


class CitationItem(BaseModel):
    id: str
    type: str
    typeName: str
    title: str


class GraphQAResponse(BaseModel):
    answer: str
    reasoning_chain: List[ReasoningStep]
    graph_data: Dict[str, Any]
    citations: List[CitationItem]


@router.post("/qa", response_model=GraphQAResponse)
async def graph_qa(
    request: GraphQARequest,
    current_user = Depends(get_current_user)
):
    """图谱增强问答"""
    user_id = str(current_user.id)

    result = graph_service.reason(
        question=request.question,
        user_id=user_id
    )

    return GraphQAResponse(
        answer=result["answer"],
        reasoning_chain=[ReasoningStep(**step) for step in result["reasoning_chain"]],
        graph_data=result["graph_data"],
        citations=[CitationItem(**cit) for cit in result["citations"]]
    )
```

### 3.3 路由注册

**文件**：`code/backend/app/api/v1/__init__.py`

```python
from fastapi import APIRouter
from app.api.v1 import auth, users, roles, knowledge, qa, graph

api_router = APIRouter(prefix="/api/v1")

api_router.include_router(auth.router)
api_router.include_router(users.router)
api_router.include_router(roles.router)
api_router.include_router(roles.permission_router)
api_router.include_router(knowledge.router)
api_router.include_router(qa.router)
api_router.include_router(graph.router)  # 新增
```

---

## 四、前端实现

### 4.1 API 集成

**文件**：`code/frontend/src/api/index.js`

```javascript
// ============ 图谱问答 API ============
export const graphApi = {
  qa: (question) => request.post('/graph/qa', { question }),
  extractEntities: (text) => request.get('/graph/entity/extract', { params: { q: text } }),
  buildGraph: (entities) => request.post('/graph/graph/build', entities)
}
```

### 4.2 页面对接

**文件**：`code/frontend/src/views/graph/GraphQAChat.vue`

核心状态：
```javascript
const expandedSteps = ref([])          // 展开的步骤索引
const hoveredNodeId = ref(null)         // 悬停节点ID（避免响应式问题）

// 切换推理步骤展开
const toggleStep = (stepIdx) => {
  const idx = expandedSteps.value.indexOf(stepIdx)
  if (idx > -1) {
    expandedSteps.value.splice(idx, 1)
  } else {
    expandedSteps.value.push(stepIdx)
  }
}
```

推理步骤展开模板：
```html
<div
  v-for="(step, stepIdx) in msg.reasoningChain"
  :key="stepIdx"
  class="reasoning-step"
  :class="{ 'is-expanded': expandedSteps.includes(stepIdx) }"
  @click="toggleStep(stepIdx)"
>
  <!-- 步骤内容 -->
  <div v-if="expandedSteps.includes(stepIdx) && step.details" class="step-details">
    <div v-if="step.details.search_query" class="detail-item">
      <span class="detail-label">搜索词：</span>
      <span class="detail-value">{{ step.details.search_query }}</span>
    </div>
    <div v-if="step.details.knowledge_found?.length" class="detail-item">
      <span class="detail-label">找到的知识：</span>
      <div class="knowledge-list">
        <div v-for="(title, idx) in step.details.knowledge_found" :key="idx">
          {{ idx + 1 }}. {{ title }}
        </div>
      </div>
    </div>
  </div>
</div>
```

---

## 五、测试

### 5.1 后端测试

```bash
# 重启后端（重要：清除缓存）
cd code/backend
taskkill //F //IM python.exe
find . -type d -name __pycache__ -exec rm -rf {} +
python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000

# 测试 Graph API
curl -X POST http://localhost:8000/api/v1/graph/qa \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  --data-raw '{"question": "test"}'
```

### 5.2 前端测试

```bash
# 启动前端
cd code/frontend
npm run dev

# 访问页面
http://localhost:3000/assistant/graph
```

### 5.3 验证推理详情展开

1. 提问一个问题
2. 查看推理路径中的步骤
3. 点击步骤右侧箭头或整行
4. 确认展开后显示"搜索词"和"找到的知识"列表

---

## 六、Bug修复记录

| 日期 | Bug | 原因 | 修复方案 |
|------|-----|------|----------|
| 2026-04-27 | 图谱节点悬停乱跳 | `hoveredNode` 存储响应式对象 | 改用 `hoveredNodeId` |
| 2026-04-27 | 图谱节点悬停乱跳 | CSS `scale(1.1)` 与 `translate()` 冲突 | 移除问题 CSS |
| 2026-04-27 | 推理详情无法查看 | Pydantic 缺少 `details` 字段 | 添加 `details: Dict[str, Any]` |
| 2026-04-27 | 图谱问答 404 | Python `__pycache__` 缓存 | 清除缓存后重启 |

---

## 七、常见问题

### Q1: 实体提取不准？

A: 当前使用规则匹配 + LLM 辅助，规则覆盖的法律类关键词较全，其他领域可扩展 `entity_patterns`。

### Q2: 图谱节点位置固定？

A: 当前使用简单圆形布局算法 `_layout_nodes`，后续可引入 force-directed layout 优化。

### Q3: 推理链步骤固定？

A: 当前实现为 4 步固定流程，未来可扩展为动态步数。

### Q4: 推理详情不显示？

A: 检查后端是否重启，确认 `ReasoningStep` 模型包含 `details` 字段。

---

## 八、后续优化

1. **Neo4j 集成**：使用 Cypher 查询实现真正的图遍历
2. **多跳推理增强**：支持自定义跳数
3. **实体关系自动抽取**：从知识内容中自动抽取
